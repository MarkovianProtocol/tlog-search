# Hold until 2026-09-24 — Hans-Christoph Steiner, F-Droid binary transparency log

**Do not send before the 24th.** The whole point is arriving with a fortnight of
observations rather than a proposal. Sending early makes it a pitch.

**Venue:** an issue on `guardianproject/binary_transparency_log`.
**Him:** Debian Developer, Guardian Project, GitHub `eighthave` (2009, 515
followers), Mastodon `@eighthave@librem.one`. Real, verified.

**Why him:** he wrote the gap down himself and never resolved it. The btlog
README, unchanged since 2017: *"stored in a git repo, which serves as an
imperfect append-only storage mechanism."* F-Droid's Security Model page: *"In
order to defend against an attacker that holds the signing keys for the app
repository, there must be a trustworthy source of information to compare
against."* Both quotes verified at source on 2026-09-10.

**Scope changed 2026-09-10 after drafting:** the watcher now covers four logs,
not one — the canonical f-droid.org log on GitLab, guardianproject's, and the
two whose subjects are third-party binaries (Google's Android SDK, Gradle). The
draft below still says one. Rewrite it around the SDK and Gradle logs: those are
the case where his own practice is already about somebody else's binaries, which
is a better hook than his own index.

Also: the first observation (tree size 7957) paired the f-droid.org index with
guardianproject's log, which covers a different app repository. Wrong pairing,
corrected at 7958. Do not cite 7957 as an observation of the index's log.

**Before sending, check:**

- `~/fdroid-watch/watch.log` on Neo — how many clean daily runs, and did any
  short-read guard fire. Replace `N` with the real count.
- Whether `entry.jar` ever changed during the run. If it did, say so and give
  the dates; that is the most interesting thing we could possibly have.
- Whether the btlog head advanced as expected. A gap is worth mentioning; do not
  imply anything by it.
- Re-verify both quotes still read the same. If he has edited the README in the
  meantime, the opening line is wrong and the draft needs rewriting.

**Do not** lead on timestamping — OpenTimestamps already gives him that free and
he knows it. The ground we hold is non-equivocation and an independently held
copy. Do not offer a service, do not ask for anything, do not mention a product.

---

Your README has called this repo an imperfect append-only store since 2017, and the security model says an attacker with the repo signing keys is only caught against a source to compare with. Both still true.

There's an outside copy now. Since 10 September, `entry.jar`'s digest recorded daily, paired with this log's head at the same moment, into an RFC 6962 log cosigned by eight independent witnesses and anchored to Bitcoin. N observations. Nothing of yours that wasn't already public.

The pair catches a force-push here, or a mirror serving one index to one reader and another to the next. It doesn't do timestamping — OpenTimestamps gives you that free — and says nothing about whether the index is right.

One line in `fdroid btlog` makes you the issuer instead of me. Strictly better: my copy is worth what any outsider's is, which is less than yours.
